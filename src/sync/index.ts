import {
  deleteOnRemote,
  enabled,
  getUnknownFromRemote,
  pullFromRemote,
  pushToRemote,
  updateFromRemote
} from "src/sync/sync";

export const sync = {
  getUnknownFromRemote,
  pullFromRemote,
  updateFromRemote,
  pushToRemote,
  deleteOnRemote,
  enabled,
};
